# challenge name(s); defaults to all compatible challenges
# challenge: [c1,c2]

# conventions + format
protocol: aido2_db18_agent-z2
user-label: "challenge-aido_LF-SpectralDropoutCNN"
user-payload: {}
description: |
    Pose estimate from CNN and simple PID controller afterwards.
